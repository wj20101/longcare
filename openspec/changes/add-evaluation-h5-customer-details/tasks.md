## 0. 补齐规格

- [x] 0.1 按已确认方案补齐 `device-h5-evaluation-flow` delta，覆盖上传成功清理设备/进度页、结果页正常返回及完成导航去重；保留等级和接口契约，通过 OpenSpec 严格校验并人工确认与 proposal 范围一致，不以 CLI 文件齐全状态代替内容核对。

## 1. 桥接与容器

- [x] 1.1 在现有 NativeBridge 和 WebViewActions 中新增客户详情方法及可选回调，复用主线程与单次导航保护；单测覆盖有效/非法 ID、关闭交错、后台、释放和无业务回调。
- [x] 1.2 在 WebView 路由中显式启用表单和报告的客户详情能力，保留关闭语义；路由测试覆盖默认禁用、两类评估入口启用及保存恢复，普通网页/隐私不跳转。

## 2. 客户详情导航

- [x] 2.1 用现有 Navigation 3 栈操作替换 H5 为客户详情；同一客户详情来源只关闭 H5 并刷新，不同客户保持来源数据隔离，移除未提交旧实现中的固定 HOME 及结果清理逻辑；测试覆盖结果页/详情来源、同客户/不同客户、失败重试、重复回调、顶部/系统返回和恢复，验证来源客户与 recordId 不被覆盖。

## 3. 登记与 SDK 页面关闭

- [x] 3.1 受影响销售链路使用可保存页面栈，填写进入确认时替换填写页，确认成功后替换为提交结果页，移除返回修改和固定首页逻辑；测试直接断言页面序列，并覆盖提交失败留页/重试、取消清理、拍照返回保留数据及成功后评估参数完整。
- [x] 3.2 核对并调整 SDK 扫描/进度状态切换、普通取消及上传完成的 entry 清理，完成动作单次消费且保留结果上下文；测试覆盖成功后设备/进度页不在返回路径、上传失败留页、结果退出正常弹栈、后台/重建/重复和迟到回调不跳走，验证会话仅释放一次。

## 4. 验证与文档

- [x] 4.1 使用离线 H5 实际调用数字及数字字符串参数，验证桥接、参数拒绝、详情显示、正常返回及原关闭用途；串联登记失败/成功、SDK 模拟上传、结果、H5、详情并断言已关闭页面不再出现，不依赖 QLZ 或提交线上业务数据。
- [x] 4.2 执行 focused tests、完整本地 preflight、Debug/Release 构建及 Lint；检查 R8 后新增方法保留并在可用设备上验证混淆包调用，未执行项如实记录，不放宽门禁。
- [x] 4.3 同步产品概览、页面地图、H5 集成契约和 OpenSpec 索引/任务状态，删除旧“详情/结果固定回首页”和“返回填写修改”的适用说明；通过文档检查、OpenSpec 严格校验及 git diff --check，确认无额外导航框架或临时产物。

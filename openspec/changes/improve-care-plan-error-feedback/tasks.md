## 1. 护理接口空值契约

- [x] 1.1 为 TodayOrder/OrderInfo 添加合成响应契约测试，经真实 Moshi/Retrofit 链路覆盖已确认字段的 null、缺失、正常值和非法类型；验证旧模型的显式 null 解析失败可复现，不使用客户隐私数据。
- [x] 1.2 仅修正设计列出的可空字段及直接 UI/实体映射消费点；通过 1.1 测试及相关模块编译，核对缺失坐标未变成有效零点、身份校验未放宽、Room schema 无变更。

## 2. 详情错误提示与恢复

- [x] 2.1 添加最小错误到字符串资源映射，在两个订单详情 ViewModel 的详情加载分支接入；单测验证业务原文/空白兜底、连接、超时、HTTP 5xx/其他 HTTP、数据异常及未知异常，不暴露原始异常。
- [x] 2.2 补充详情 ViewModel 恢复测试，验证成功、失败后重试成功、取消不报错，并运行现有会话失效契约测试；确认未修改工单开始、路由、planId 或全局重试逻辑。

## 3. 整体验收与文档

- [x] 3.1 运行受影响单测、`preflight_local.sh --full`、App Lint、Debug/Release 构建，核对可空模型的直接消费点及混淆构建；保留现有 Lint、签名、R8 门禁，不将构建缓存命中描述为重新执行测试。
- [x] 3.2 通过离线 UI 测试或模拟器验证错误提示可见、重试恢复和返回正常；真机可用时仅复现读取详情并核对脱敏错误类别，未获得实际响应则明确本次现场根因尚未确认，不提交服务数据。
- [x] 3.3 同步产品概览中护理失败/恢复行为、必要的页面说明及 OpenSpec 状态；文档校验、严格规格校验和 `git diff --check` 通过，不新增过程日志文档。

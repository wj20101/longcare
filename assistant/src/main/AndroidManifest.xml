<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <application
        android:name=".AssistantApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/assistant_data_extraction_rules"
        android:fullBackupContent="@xml/assistant_backup_rules"
        android:icon="@drawable/ic_assistant"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LongCareAssistant"
        android:usesCleartextTraffic="false">
        <!-- ML Kit is initialized explicitly only after privacy consent. -->
        <provider android:name="com.google.mlkit.common.internal.MlKitInitProvider" tools:node="remove" />
        <!-- Only the launcher is exported. No deep links, NFC filters or external route extras. -->
        <activity android:name=".AssistantActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

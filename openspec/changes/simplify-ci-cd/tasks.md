## 1. CI 测试覆盖与职责精简

- [x] 1.1 删除伪模块影响输出与固定 App 测试过滤，接入完整 JVM 业务测试集合；用 Gradle task graph 和各模块 XML 结果核对覆盖，包含销售、Core/Data 与现有 Feature 测试。
- [x] 1.2 将检测脚本收敛为冒烟选择逻辑，删除旧命名兼容层；用临时仓库场景覆盖平台/CI 文件、纯业务、文档、手动运行、不可用基线，确认手动运行不漏测。
- [x] 1.3 用真实启动/页面冒烟替代 ExampleInstrumentedTest，排除需真实账号/硬件的 opt-in 用例；在模拟器执行所选集合并保留结果，不将跳过当通过。
- [x] 1.4 删除普通 CI 的 Debug AAB 和无消费 Baseline APK 上传，合并重复报告；通过工作流测试确认 Debug APK、失败诊断和各模块测试报告仍可获取。

## 2. 发布流程与旧实现删除

- [x] 2.1 删除 Tag 触发/拒绝分支、Release 中 Baseline 生成及临时 SDK 切换选项、无引用旧 run-baseline-profile.sh；引用扫描和发布契约测试确认无残留且独立 Baseline 与 SDK 配置仍有效。
- [x] 2.2 删除 Release 无消费的 Debug APK 构建及同配置重复静态门禁，保留必要 Lint/厂商风险检查；测试无成功源提交 CI 时阻断、按 SHA 查询不受最近 20 次限制。
- [x] 2.3 版本远端写入移至正式构建及产物校验之后，发布使用不自动取消的稳定并发组；替身测试覆盖构建失败无推送、并发配置、分支前进拒绝强推、发布失败不覆盖历史版本。
- [x] 2.4 同步版本文档并验证标签/提交/包版本一致；保留签名、R8、导出组件、主应用 APK/AAB、mapping 和校验和检查，正式构建验证产物完整性。

## 3. 存储、共享环境与守卫清理

- [x] 3.1 删除三个工作流中的缓存清理 job，仅保留定期维护；用工作流行为测试确认维护失败不会污染构建结果。
- [x] 3.2 修复缓存近期保护和报告至少 7 天保留，移除提前删除保护期内产物的分支；API 替身与 dry-run 测试覆盖超限、到期、近期访问和删除失败，不执行远端破坏性清理。
- [x] 3.3 精简共享环境 action 门禁开关、僵化步骤名称/文本断言及已删除功能的测试；执行守卫测试，确认必要安全、失败阻断、KVM 等行为仍受保护。
- [x] 3.4 收窄腾讯迁移验证触发路径，统一独立 Baseline 的 KVM 等待；路径及 KVM 回归测试通过，保留手动专项和健康监测。

## 4. 文档与整体验收

- [x] 4.1 更新现有 CI 文档、入口说明、OpenSpec 配置中的验证描述和相关引用，不新增执行日志；文档校验、严格规格校验、git diff --check 通过，逐项核对删除清单。
- [x] 4.2 运行完整工作流守卫、本地 preflight、JVM 集合和 Release 构建，汇总真实执行/缓存/跳过边界；失败不得以放宽 Lint、签名、R8 或删除业务测试处理。
- [ ] 4.3 获得推送授权后验收 GitHub Actions 的构建/冒烟/报告结果并对比耗时；正式发布和远端存储删除另行授权，未执行的线上项保持未完成。

## 1. 页面与文案

- [x] 1.1 信息提交结果页删除横排/大字号条件分支，使用同宽纵向按钮组、间距 10dp，调整为进行评估在上、确认并返回在最下方，保留现有回调；补充普通字号和大字体布局顺序/点击回归测试。
- [x] 1.2 评估结果页将现有纵向操作归为间距 10dp 的同宽按钮组，调整为查看评估报告在上、完成在最下方；回归布局顺序、等级、加载、失败、刷新及报告可用状态，确认点击行为不变。
- [x] 1.3 将销售设备流程客户端文案中的“评估设备”简化为“设备”（图片描述“健康评估设备”改为“设备”），不改资源 key 和扫描所得真实名称；更新文案测试并搜索确认无遗漏。

## 2. 验证与文档

- [x] 2.1 运行受影响销售专项测试、完整本地 preflight、Debug/Release 构建与 Lint；执行离线 UI 的普通字号、大字体和短屏检查，不新增线上客户或触发检测上传，记录实际验证范围。
- [x] 2.2 同步产品概览及任务状态，确认正式环境未部署的记录和正式配置不冲突；运行文档检查、OpenSpec 严格验证及 git diff --check。正式设备全链路仍归原 Token 变更验收，不标记为已通过。

本次仅为现有 Compose 布局与字符串调整，无跨模块、安全、迁移或新增依赖复杂度，按 schema 条件省略 design.md；不新增布局框架，不改业务状态和环境配置。

验证范围：12 项专项通过（含 6 项 Robolectric Native Graphics 布局/点击用例，320dp 宽，普通字号、2 倍字体和 320dp 短屏）；完整 preflight 通过，App 438 项单测重新执行、零失败/跳过，其余模块测试命中缓存。Debug、Release/R8、Lint、AndroidTest Kotlin 编译通过；Lint 警告清单无新增，仅保留既有厂商 Aligned16KB / TrustAllX509TrustManager。未安装新包至真机、未执行在线检测上传，不替代正式部署后的联调。

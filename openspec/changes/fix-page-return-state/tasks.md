## 1. 服务完成退出

- [x] 1.1 完成页顶部返回、系统返回和完成按钮统一弹出当前 entry，删除仅为按钮清理存在的依赖；页面测试验证回到同一 Home entry、保留状态且不恢复服务步骤，倒计时退出回首页测试保持通过。
- [x] 1.2 将必要的选中项目清理并入现有服务结束成功处理，删除旧按钮清理路径；单测验证成功清理、失败保留、立即退出不漏清理及完成摘要完整，检查首页恢复后订单状态更新。

## 2. 销售页面返回状态

- [x] 2.1 客户详情在当前 entry 进入前台时查询最新数据，合并重复加载触发并删除确认无用的刷新标记；离线页面测试覆盖首次进入、评估后逐级返回、H5 同客户复用、普通重组不重复查询、查询失败重试及不同客户隔离。
- [x] 2.2 客户列表区分首次加载、条件变化与普通返回，保留分页和滚动位置；ViewModel 和页面测试覆盖多页返回、空结果、失败重试、条件变化及数据丢失后的恢复，不增加结果关联机制。

## 3. 集成验证与文档

- [x] 3.1 执行相关导航/NFC/销售 focused tests、完整本地 preflight、Debug/Release 构建与 Lint；补充离线真实 Navigation 3 页面返回测试，不提交线上检测或问卷，不放宽门禁，记录未执行的真机范围。
- [x] 3.2 同步产品概览、页面地图、协作约定中的倒计时业务例外和 OpenSpec 索引/规格；通过文档检查、OpenSpec 严格校验及 git diff --check，核对只涉及三项已确认调整并保留既有未提交修改。

验证边界：本地完整 preflight、专项测试、Debug/Release（R8）构建及 Lint 通过；API 36 模拟器的完成页退出、Navigation 3 状态恢复和客户搜索共 15 项通过。销售页面使用真实 Navigation 3 与离线仓库替身覆盖往返链路。首页恢复刷新沿用既有 RESUMED 查询。未执行真机 NFC 签退、QLZ 检测或线上问卷提交，不代表厂商硬件验收通过。

## 1. 契约与安全前置核查

- [x] 1.1 核对 GetCheckResult 的 POST 路径、id/可空 recordId 与 pgResult/pgUrl，并补充契约测试；完成由评估 H5 主动关闭确定，不另行查询完成状态。
- [x] 1.2 核对直接注册接口的可用性与注册/移除时序；明确移除来源限制后所有页面及 iframe 都可调用，未来新增敏感方法须独立授权。

## 2. 最小原生关闭接口

- [x] 2.1 在统一容器首次加载前注册名为 NativeBridge 的最小对象，仅暴露无参数、带 @JavascriptInterface 的 closeWebView；测试新旧内核可直接调用，客户端不注入 JS 包装或额外业务方法，后续可直接在 NativeBridge 中新增注解函数。
- [x] 2.2 实现主线程、前台、当前容器/source entry、单次关闭与销毁保护；单测和真实 WebView 测试覆盖重复调用、后台后恢复、退出/换页、迟到回调、渲染失败及资源释放。
- [x] 2.3 删除 isSafeUrl、域名白名单与相关导航/请求拦截；覆盖任意来源初始加载、跨域导航和 iframe 调用，保留 TLS 等平台安全配置。
- [x] 2.4 回归普通 H5、报告、协议和隐私容器的 H5 主动关闭、原生返回及 Navigation 3 来源状态；验证不清空首页、不改变隐私同意、普通网页不触发评估完成。
- [x] 2.5 删除替换后明确无用的 JS 包装、消息通道、文档凭证和能力选择代码，删除独立关闭 Policy 和加载失败禁止关闭的耦合，移除不适用的来源拦截测试；以引用搜索、编译、Lint 和回归验证无双轨实现，并保留其他功能仍使用的依赖与渲染保护。

## 3. 设备与 H5 连续流程

- [x] 3.1 拆分设备上传完成与业务评估完成，保存客户/会话及待打开请求；测试 100% 不跳转、上传成功仅打开一次、失败重试和后台恢复。
- [x] 3.2 评估 H5 主动关闭直接显示完成页，系统返回回来源页；所有网页不增加返回结果关联，覆盖重复关闭、恢复、普通网页和首页保留。
- [x] 3.3 完成页有设备记录时请求 GetCheckResult，纯表单直接重新请求客户详情获取结果文本和报告地址；处理空值/失败/手动刷新与换客户取消旧请求，不读取缓存旧值、不增加失败兜底链、不二次核实完成、不重复设备上传。
- [x] 3.4 完成页显示接口等级文案，覆盖设备/纯表单两条直接查询分支；测试 A级、中文等级、空等级、小屏/大字体及返回首页。
- [x] 3.5 评估表单和报告显式采用无原生标题/返回栏展示，H5 填满系统安全内容区；展示参数与评估完成用途分离，其他网页和隐私容器保持原布局，不按标题或 URL 猜测用途。
- [x] 3.6 在现有页面中完成最小沉浸式适配：白色背景延伸至系统栏，使用 AndroidX 管理可读图标，离开后恢复默认样式；验证安全区、键盘和新旧系统，不增加框架或 JS。

## 4. 验收与文档

- [x] 4.1 使用隔离 mock 完成“设备上传 → H5 提交与关闭 → 原生等级完成页”及取消/重试链路；执行受影响单测、Navigation 3 状态恢复测试与 WebView instrumentation。
- [x] 4.2 按 android-cli 技能验证 API 24 旧内核、当前 WebView 80 真机与现代内核，记录实际版本和覆盖限制；真机只使用受控页面，不重复提交客户数据、不降级/清空系统组件。缺少设备时如实保持未验收。
- [x] 4.3 UI 调整后重新真机验收：表单和报告仅保留 H5 标题；成功弹窗确认后刷新并停留，点击 H5 左上角返回才关闭；评估入口关闭后显示接口等级，报告关闭仅返回来源页，系统返回保留首页。优先复用已授权测试数据，不为 UI 验收重复提交；新增提交需确认授权。
- [x] 4.4 同步产品、路由和 QLZ 集成文档；按用户确认升级 AGP 9.4.1，串行运行完整 preflight、双应用构建/Lint、关闭接口 instrumentation、warning allowlist、助手隔离、严格 OpenSpec 校验和 diff 检查，不放宽既有门禁。
- [x] 4.5 按用户确认补齐 BLE 前台定位授权和全版本定位开关检查；覆盖实际权限数组、粗略定位不足、拒绝/恢复、后台/退出释放并重新构建与真机验收，不新增后台定位、neverForLocation 或位置采集。
- [x] 4.6 补充无标题栏布局、用途恢复和按钮语义测试，回归系统返回、重复/迟到 JS 关闭、报告与隐私隔离、网络/渲染异常退出；检查旧内核和现代设备的安全间距、键盘、大字体及进度/错误显示。
- [x] 4.7 同步产品/路由/QLZ 文档，移除“成功确认必须自动关闭”的错误验收要求；串行执行受影响测试、完整 preflight、双应用构建/Lint、既有 allowlist、隔离、严格 OpenSpec 和 diff 检查，不放宽门禁。后续依赖/PR 按独立的 `upgrade-dependencies-and-review-prs` 执行。

## 验收结论

20 项任务已完成。设备测量上传后自动打开 H5；成功弹窗确认只刷新，H5 左上角返回实际调用 NativeBridge 后进入原生完成页。纯表单结果改为重新查询客户详情，设备结果继续 GetCheckResult；真实接口只读测试确认显示 A级，修复后未重复提交问卷，不冒称重新执行整条提交旅程。

完整 preflight、双应用 Debug/Lint、既有 allowlist/隔离与严格规格检查通过。关闭接口在 API 24 / WebView 52、WebView 80 真机及现代内核受控验证通过；最终 Pixel 的关闭、展示、渲染退出与 Navigation 3 共 25 项通过，API 24 的接口/导航 19 项通过。另有沉浸式、大字体、键盘及结果分支专项，不合计重复轮次为独立覆盖，也不宣称穷举所有内核。

BLE 权限回归覆盖 API 24/30/31/33，真实设备完成授权恢复、扫描连接、采样上传及 H5 跳转。完整硬件异常矩阵仍由 use-qlz-custom-evaluation-ui 的未完成任务跟踪。后续依赖升级单独实施。

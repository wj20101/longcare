<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
      Default-deny backup policy:
      LongCare contains service/order/location/identity related data.
      We explicitly exclude all app-private domains from cloud backup.
    -->
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <!-- Keep the same policy for device-to-device transfer. -->
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>

## 1. 一次性凭证交付

- [x] 1.1 整合初次启动与恢复的 Token 获取/一次性交付，移除已用 `checkToken` 和 `restart(currentToken)` 重放路径；ViewModel/session/controller 单测断言每次新授权先调用 GetCheckToken、每个启动请求只消费一次，普通扫描与重连不新增授权。
- [x] 1.2 调整检测页返回、权限恢复和宿主重建后的新会话入口及按钮加载状态；离线 UI/controller 测试覆盖“检测页返回设备页再搜索”、授权失败重试和页面重建均使用新 Token，现有会话继续操作正常。
- [x] 1.3 对 Token 请求增加当前流程取消/失效与重复点击保护；协程单测覆盖快速双击、切换客户、退出后的迟到返回、旧请求 finally、空 Token 和接口失败，确保不启动旧会话或覆盖新状态。

- [x] 1.4 按评审精简 ViewModel：仅保留流程 Job 与自动恢复标记，删除重复序号/活动状态，以对象身份消费启动请求，Token loading 与通用 loading 分离；复跑退出、迟到回调、切换客户、重复消费及完整本地门禁，不削减既有行为。
- [x] 1.5 深入 review 并清除多套授权入口、会话隐式重建、跨层上传恢复标记、无调用的厂商 WindowInsets 补丁/旧文案过滤，以及无消费者的设备名/进度/厂商报告字段；使用单一会话授权入口和前台消费，覆盖后台等待、无会话拒绝、重复授权及原记录恢复，并复跑本地门禁和构建。

## 2. 上传鉴权恢复

- [x] 2.1 核对固定 AAR 的上传鉴权码和重新授权调用，新增最小分类与保留原记录的授权后重传路径；专项测试覆盖 401/2001、普通网络失败、原数据/recordId 不变、无重新测量、无 driver 清空，以及一次自动恢复上限和迟到回调。
- [x] 2.2 验证凭证恢复失败时不重放旧 Token、不伪报成功、不无限刷新，上传成功后仍只触发一次 GetCheckResult 并由用户打开报告；使用真实 ViewModel/session 的离线流程测试验证，不提交线上数据。

## 3. 正式环境切换

- [x] 3.1 删除 `CheckIml.setTestMode(...)`、`isTestMode` 及 Gradle 中无用的测试模式开关，保留现有 SDK key 值；离线测试和源码检查验证没有测试/本地模式调用或环境回退，本 AAR 默认正式基地址及上传路径正确，Debug/Release 配置一致。
- [x] 3.2 同步 Release 配置检查、脚本测试和已确认 key 的命名/说明，去掉失效的测试模式输入；运行相关脚本回归，确认不再误报测试模式开启，未解决的厂商风险与签名/质量检查仍有效。

## 4. 验证与说明

- [x] 4.1 运行受影响专项、`preflight_local.sh --full`、`:app:lintDebug :app:assembleDebug :app:assembleRelease` 及 Lint warning 守卫，说明实际执行/缓存边界；检查无新增敏感日志、依赖或门禁放宽。
- [x] 4.2 更新 QLZ 集成文档、必要页面/发布说明及活动变更索引，明确一次性凭证和正式环境配置已取代旧复用及测试环境；历史风险记录不冒充当前配置。文档校验、`openspec validate --all --strict --no-interactive` 和 `git diff --check` 通过。
- [ ] 4.3 获得正式环境测试客户与提交授权后，真机验证正式服务授权/上传、连续两次检测、取消返回再检测、上传结果/报告回退；若能重现鉴权失效，验证原记录换凭证恢复。不人为制造生产故障、不输出 Token；未取得设备/授权或未能验证厂商恢复时保持对应验收未完成并说明边界。

2026-09-21 真机进展：Pixel 10 / Android 17 安装 `df9451f2` 对应 Release 1.0.6（65），测试客户 QlzToken0921A 登记成功，首次授权/扫描及取消后重新进入、再次扫描通过。连接 BMS105695 后显示设备未授权；客户端映射对应固定 AAR `error_check_device_au = 21`，不是 Token 错误 100/102。用户随后确认正式环境尚未部署，联调暂缓、保留正式配置；设备绑定或服务部署的具体错误根因尚未证实，待部署后复验。尚未测量或上传，连续两次检测、结果/报告及原记录恢复未验收。截图与旅程报告留在 ignored build 产物，不抓取或记录 Token。

## 1. 迁移两项本地检测

- [x] 1.1 核对助手代码、资源、依赖及 NFC 事件订阅引用，划定迁移与删除清单；以引用搜索确认主应用仍使用的共享能力不在删除范围，保留其他任务未提交修改。
- [x] 1.2 新建轻量读卡 Feature，迁移 NFC/R65C UI、输入解析状态与清空复制行为，增加显式模式选择；以编译和 focused tests 验证支持 NFC 的手机也能选择 R65C、有效/无效数据均可恢复。
- [x] 1.3 接入主应用平台读取与生命周期，确保模式独占、后台/退出释放且不调用业务提交；以测试验证 NFC/HID 退出后不消费输入、不触发签到或上传，系统设置返回刷新 NFC 状态。

## 2. 登录页大 Logo 长按确认入口

- [x] 2.1 将入口替换为中央大 Logo 的标准长按，普通点击无动作，显式关闭触觉反馈和点击/长按的波纹及按压视觉效果；删除摇动判定器、传感器监听、阈值、冷却及废弃测试，同步 CI 测试选择器和守卫引用，搜索确认无残留依赖。
- [x] 2.2 保留隐私同意后登录页前台可交互限制及 Material 3“打开助手”单实例弹窗，正文说明本地 NFC/R65C 用途；UI/生命周期测试覆盖短按、长按、移动取消、持续按住、无震动、Logo 按压像素不变、取消、返回、遮挡、提交登录及后台恢复，不发生意外导航。
- [x] 2.3 保留现有 Navigation 3 检测路由和登录表单状态；以长按入口重新验证确认只入栈一次、检测返回登录页、输入不丢失，过期确认和重复长按不跳转。

## 3. 删除助手并收敛构建发布

- [x] 3.1 删除 `:assistant` 应用、模块注册及确定仅为助手使用的测试代码、资源和依赖；以引用检查、模块边界检查和主应用 Debug 构建验证无悬空依赖，正式业务共享能力保留。
- [x] 3.2 移除双包脚本与 CI/Release 助手构建、上传、校验和映射配置，迁移仍必要的业务隔离守卫；以工作流/脚本测试及产物断言验证新流程仅要求主 APK/AAB、缺失主产物仍失败、版本只递增一次且不操作历史 Release。
- [x] 3.3 将当前入口文档及主规格从摇动同步为大 Logo 长按、无震动，并保持已完成的单应用发布与双包规格退役；通过 OpenSpec 严格校验、文档检查和搜索确认当前指引一致，历史档案保持原意。

## 4. 集成与正式包验收

- [x] 4.1 在长按入口修改后重新运行相关 JVM/UI 测试、Lint、架构及发布质量检查，构建主应用 Release APK/AAB；验证签名、包名、不可调试、R8/资源压缩和校验信息，保留既有厂商风险警告，不扩大 keep 或放宽门禁。
- [x] 4.2 使用 Android CLI 安装 Release，并结合人工操作验证大 Logo 长按弹窗、短按无动作、无震动、取消、重复长按、后台恢复和返回，以及非入口区域隔离；自动输入未完成的交互由用户手动验收，用户已确认真机测试完成，不以旧摇动或模拟测试代替。
- [ ] 4.3 使用真实 NFC 标签和 R65C 验证两种模式、复制/清空、切换与资源释放，并回归登录和正式业务读卡隔离；缺少硬件时明确列为未验收，不新增业务数据或宣称全部通过。

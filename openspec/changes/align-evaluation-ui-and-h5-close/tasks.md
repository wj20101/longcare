## 1. 核对设计与平台事实

- [x] 1.1 从三张蓝湖稿读取颜色、尺寸、文案及手握/沙漏素材，核对已有资源；以原图预览和设计标注确认资源与比例一致。
- [x] 1.2 核对 Demo/AAR 的五指、开始、进度事件顺序与准备展示映射；以对应代码/API 证据及状态映射测试确认，不修改采集协议。
- [x] 1.3 使用 android-cli 核对 AndroidX WebKit 稳定版本和两项桥接功能检测，加入版本目录依赖；以依赖解析和编译成功验证。

## 2. 检测视觉与状态

- [x] 2.1 实现三态展示及可测试的准备计时，覆盖接触丢失、提前进度、重组、后台、充电、退出和重试；以时间可控的 focused unit tests 验证。
- [x] 2.2 按标注更新背景、标题、单卡片、手握灯、沙漏和进度，保留异常恢复；以三个确定性 mock 状态的模拟器截图逐项对照设计。
- [x] 2.3 删除新布局替代且确认无引用的旧组件、文案和资源；以引用检索、编译及已有销售 UI 测试验证无误删。
- [x] 2.4 更新检测 mock 和大字体测试，覆盖短屏、大字体、充电、上传失败及返回；以 Compose instrumentation 通过且操作可达验证。

## 3. H5 关闭桥与 Navigation 3

- [x] 3.1 原方案的评估用途隔离已完成验证；用户追加要求后由第 5 节统一 H5 接口取代，不再保留专用开关。
- [x] 3.2 实现精确 HTTPS 来源限定和文档起始注入，公开唯一 `window.NativeBridge.closeWebView()`，验证主 frame/固定消息/运行时能力；以来源规则单测及非可信输入测试验证。
- [x] 3.3 将关闭事件接入当前 source-bound entry，处理主线程、去重、非栈顶和释放；以 Navigation 3 返回测试确认首页/来源页保留且重复调用无影响。
- [x] 3.4 修正 WebView 重组重复加载，销毁时释放脚本/监听器/WebView；以重定向后重组、页面重建及销毁后的调用测试验证。
- [x] 3.5 在测试源集中搭建受控 HTTPS WebView 内容执行真实 JS 方法，覆盖正常关闭、同源/跨源 iframe、未授信重定向、普通网页、重复/迟到回调及能力不足；以 instrumentation 通过验证，不关闭 TLS 校验。
- [x] 3.6 按追加确认补齐通用网页和隐私弹窗的渲染退出提示、桥失效及 WebView 释放；以受控回调 instrumentation 和 Lint allowlist 验证，保留隐私同意与原生返回行为。

## 4. 集成验收与文档

- [x] 4.1 使用隔离模拟器完成 mock 检测三态与 H5 关闭至来源页面的完整流程；通过 android-cli 检查布局/截图，记录未覆盖的真实 BLE 和真实 H5 限制，不访问真实客户服务。
- [x] 4.2 同步 QLZ 集成、页面路由及依赖文档，说明准备计时和唯一 JS 契约；以文档链接检查及代码事实核对验证。
- [x] 4.3 串行执行受影响单测、instrumentation、`:app:lintDebug :app:assembleDebug`、完整 preflight、助手隔离与 Lint allowlist；确认未放宽任何发布或安全门禁。
- [x] 4.4 执行 `openspec validate --all --strict --no-interactive` 和 `git diff --check`，审查仅包含本任务必要变更并保护其他未提交工作。

## 5. 所有 H5 统一关闭（用户追加确认）

- [x] 5.1 移除表单专用路由/动作/桥命名，所有路由 H5 注册统一关闭方法，保留精确 HTTPS 来源、主 frame、前台和去重保护。
- [x] 5.2 隐私协议网页弹窗复用统一容器并开启 JavaScript，关闭只 dismiss 网页；释放、渲染异常、能力不足提示与普通网页一致。
- [x] 5.3 更新文档及单测，真实 WebView 测试覆盖报告/普通网页、隐私弹窗关闭不等于同意、重复调用、销毁后回调、来源隔离和 Navigation 3 返回；运行受影响构建、Lint、preflight 及严格规格校验。

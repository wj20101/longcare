# 销售首页最新蓝湖设计 QA

日期：2026-07-26
设计稿：蓝湖 `image_id=94c5e809-258f-4fd7-964e-6b0ec22840ee`
验证设备：Android API 37 真机，1080 × 2424 px，density 2.625（约 411 × 923 dp）

## 对比目标与状态

- 蓝湖原图：`/private/tmp/lanhu-sales-home-latest-original.png`，1500 × 2906 px。
- 同宽归一化设计图：`/private/tmp/lanhu-sales-home-latest-1080w.png`，1080 × 2092 px。
- 最终真机实现：`/private/tmp/longcare-sales-home-latest-final-hd.png`，1080 × 2424 px。
- 蓝湖画板按 2 px/dp 换算约为 375 × 726.5 dp；真机视口约为 411 × 923 dp。比较时以相同 1080 px 内容宽度归一化，纵向差异按响应式视口与 Android 系统栏处理。
- 比较状态一致：已登录销售账号、首页首屏、客户数据加载完成、底部导航选中“首页”。

蓝湖稿使用 4 条静态客户数据且不包含 Android 状态栏、手势导航区；真机实现使用测试环境 Sale API 实时数据，当前返回 1 条客户记录。客户数量、姓名、企业和用户信息差异属于预期，不作为设计缺陷。

## 完整页面对比证据

已将同宽归一化设计图与最终真机截图放入同一视觉比较输入，逐项检查头部、Banner、功能卡、客户列表和底部导航。1080 px 宽的完整对比已能清晰辨认 Logo、Banner、图标、文字、圆角和间距，因此无需再依赖会损失上下文的独立局部裁剪。

Banner 另使用蓝湖原始高清切图 `/private/tmp/lanhu-sales-home-banner-latest.png`（2712 × 960 px）核对，并以原始宽高比接入最终页面；最终全屏截图中素材细节与设计稿一致。

## 迭代比较记录

- P2：首页首轮实现复用了 678 × 240 px 低分辨率 Banner，并固定为 120 dp 高度，真机上存在清晰度和比例偏差。已替换为蓝湖 2712 × 960 px 原始切图，并按 `2712 / 960` 宽高比响应式展示。修复后证据：`/private/tmp/longcare-sales-home-latest-final-hd.png`。
- P3：首轮两个功能卡背景为纯白，与设计稿浅蓝底有差异。已调整为 `#F3F7FE`。
- 最新结构已与设计稿同步：只保留“对象登记”和“代办提醒”两个入口；内容区改为“最新客户”；底部导航为“首页 / 我的客户 / 我的”。

## 必检视觉面

### 字体与文案

- 页面标题、用户角色、功能标题、辅助文案、客户状态和底部导航的字号与字重层级匹配设计稿。
- 功能入口文案为“对象登记 / 客访登记信息”和“代办提醒 / 今日还未签到”。
- 客户状态按首页设计语义展示：未申报（橙）、待审核（蓝）、审核通过（绿）、审核不通过（红橙）。

### 布局与间距

- 顶部身份区、Banner、两个等宽功能卡、最新客户列表和底部导航的层级、边距、圆角与蓝湖一致。
- 页面在 411 × 923 dp 真机视口上保持设计宽度比例，并正确避让 Android 状态栏和底部手势区。
- 真机比蓝湖画板更高，实时数据少于静态稿，因此客户列表下方自然留白属于预期响应式结果。

### 色彩与素材

- 顶部渐变、主蓝色、功能卡浅蓝背景、状态语义色和底部选中态均与设计稿一致。
- Logo、头像、功能图标与 Banner 均使用项目正式资源或蓝湖原始素材；未使用占位图、手绘替代或近似图形。
- Banner 使用高清原始切图，不再存在缩放模糊。

### 交互验证

以下流程已在同一台真机完成：

- 点击“对象登记”进入“信息登记”页面。
- 点击“代办提醒”进入提醒列表。
- 点击底部“我的客户”进入客户列表。
- 点击底部“我的”进入个人中心。
- 返回首页后页面内容、选中态和系统安全区显示正常。

## 最终结论

无未解决的 P0、P1 或 P2 设计问题；动态客户数据和更高真机视口造成的留白属于预期差异。

final result: passed
